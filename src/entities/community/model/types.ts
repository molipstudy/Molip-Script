export type CommunityScript = {
  id: string
  ownerId: string
  ownerLoginId: string
  sourceScriptId: string
  title: string
  rawText: string
  sharedAt: string
}

export type CommunityScriptRow = {
  id: string
  owner_id: string
  owner_login_id: string
  source_script_id: string
  title: string
  raw_text: string
  shared_at: string
}
